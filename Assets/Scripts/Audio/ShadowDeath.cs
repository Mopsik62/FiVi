using UnityEngine;
using AK.Wwise;
public class ShadowDeath : MonoBehaviour
{
    public AK.Wwise.Event ShadowDeathEvent;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
