using UnityEngine;
using AK.Wwise;
public class RobotFootstep : MonoBehaviour
{
    public AK.Wwise.Event RobotFootstepsEvent;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
