using UnityEngine;
using AK.Wwise;
public class PechkaWalk : MonoBehaviour
{
    public AK.Wwise.Event PechkaWalkEvent;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
