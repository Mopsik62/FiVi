using UnityEngine;
using AK.Wwise;
public class Footsteps_Gravel : MonoBehaviour
{
    public AK.Wwise.Event footstepGravelEvent;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
