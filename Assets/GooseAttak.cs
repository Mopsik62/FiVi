using UnityEngine;
using AK.Wwise;
public class GooseAttak : MonoBehaviour
{
    public AK.Wwise.Event GooseAttakEvent;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
